[project]
name = "ai-dnd-summariser"
version = "0.1.0"
description = "Record, transcribe and summarise Discord DnD sessions on macOS."
requires-python = ">=3.11"
dependencies = [
    "whisperx>=3.8.6",
]
